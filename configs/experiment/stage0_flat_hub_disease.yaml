experiment:
  name: stage0_flat_hub_disease_studyholdout
  stage: 0
  seed: 42

pilot:
  mode: hub_pack
  task: multiclass
  label_column: phenotype_value
  empty_as_control: true
  matrix_id: matrix-hub-disease-from-agepack-v1
  graph_id: graph-grch38-gencode38-five-role-v1
  static_feature_set: cpgpt2m_adapter_128_v1
  phenotype_family: disease
  # Betas from age pack (disease profile zip incomplete at convert time);
  # labels joined from disease_sample_info.parquet.
  train_studies: ["GSE74193", "GSE43414"]
  validation_studies: ["GSE59685"]
  external_test_studies: []
  split_id: hub-disease-from-agepack-v1

features:
  methylation:
    beta: true
    m_value: true
    epsilon: 0.001
  static_feature_set: cpgpt2m_adapter_128_v1

model:
  type: flat_deepset
  phi_layers: 2
  phi_hidden_dimension: 20
  rho_layers: 3
  rho_hidden_dimension: 10
  activation: leaky_relu
  pooling: max
  output_activation: sigmoid
  neutral_score: 0.5
  dropout: 0.0

heads:
  age:
    enabled: false
  tissue:
    enabled: true
    loss: cross_entropy
    class_weighting: train_fold_balanced

training:
  optimizer: adamw
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 1
  gradient_clip_norm: 2.0
  max_epochs: 40
  early_stopping_patience: 8
  precision: bf16-mixed
  require_cuda: true

device:
  cuda_visible_devices: "0"
  torch_device: cuda

logging:
  tensorboard: true
  wandb: false
  save_resolved_config: true
  save_environment: true
  save_checksums: true
