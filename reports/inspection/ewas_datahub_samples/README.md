# EWAS DataHub sample-info packs (unzipped)

Unpacked once from `data/raw/ewas_datahub/download/sample_*.zip`.
Those zips were **deleted after extract** to save space; re-download from
https://download.cncb.ac.cn/ewas/datahub/download/ if you need the archives again.

Each pack contains:
- `sample_*.txt` — tabular sample metadata (Cursor-indexed)
- `sample_*.RData` — same info as serialized R (ignored by Cursor)

Still missing until downloaded: `sample_cancer`, `sample_ancestry_category`, `sample_bmi`.
